const size_selected = document.getElementById("size_selected");
const color_selected = document.getElementById("color_selected");

const setSize = (size) => {
  size_selected.value = size;
};

const setColor = (color) => {
  color_selected.value = color;
};
